# 校园零工平台 Agent 执行规则

## 项目目标

在 2026-07-21 23:59（Asia/Shanghai）前交付一个可公开访问的中文 Demo。评测者应能在同一个链接内切换“学生端”和“招聘方端”，完成校园快递驿站零工从报名到模拟结算的闭环。

## 开始任何任务前

按顺序读取：

1. `docs/PRODUCT_SPEC.md`
2. `docs/DOMAIN_AND_DATA.md`
3. `docs/DELIVERY_PLAN.md`
4. `docs/QA_MATRIX.md`
5. `docs/DECISIONS.md`
6. `docs/CHANGE_CONTROL.md`

若文件之间存在冲突，停止实现并按 `docs/CHANGE_CONTROL.md` 提交变更，不得自行选择。

## 强制规则

- 产品名称为“校园零工平台”；当前 Demo 启用“XX大学”，架构保留多校园能力。
- 当前唯一招聘方为“XX大学校园快递驿站”，发布 3–5 条不同班次。
- 使用 React、Vite、TypeScript、Supabase 和 Vercel。
- 同一匿名会话内切换学生端与招聘方端；这只是 Demo 体验机制，不宣称生产级鉴权。
- 页面组件不得直接拼接 Supabase 查询或任意修改业务状态。
- 状态流转必须经过统一业务服务；关键流转还须由数据库 RPC/约束校验。
- 数据库变更只通过 `supabase/migrations/` 中的追加式 migration 执行。
- 每次状态变化必须写入事件记录，不能只覆盖当前状态。
- 金额以“分”为整数存储，时长以“分钟”存储，时间使用 `timestamptz`，界面按北京时间显示。
- 所有业务表显式启用 RLS；浏览器中只允许使用 Supabase publishable/anon key。
- 禁止将数据库密码、GitHub token、`service_role` key 或真实 `.env` 提交到仓库。
- 所有岗位、学生、招聘方均为合成演示数据；不得声称真实认证、绝对安全或真实支付。
- “已结算”始终明确标注为“Demo 模拟结算”。
- 不得在上一阶段退出条件未通过时进入下一阶段。
- G0–G2 通过前不得编写业务代码或业务 migration。

## 三方一致决策

以下材料性决策必须由产品负责人、技术架构负责人、交付与质量负责人全部无条件同意：

- 产品范围、主流程和异常流程；
- 状态机、数据结构和权限边界；
- 外部服务、视觉原则和验收标准；
- 会导致已批准契约变化的实现方案。

已批准契约内的例行实现、缺陷修复、测试补充和不改变视觉原则的细节优化无需重复表决。

## 验证要求

代码阶段至少执行：

- TypeScript 类型检查；
- ESLint；
- 单元测试；
- Production build；
- 主流程和非法状态跳转测试；
- 移动端与 PC 端浏览器验收；
- Vercel 根路由与直接子路由访问测试；
- 中国大陆无 VPN 网络下的最终链接验收。

没有可复核证据时，不得声称阶段完成。
